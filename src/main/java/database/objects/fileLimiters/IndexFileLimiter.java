package database.objects.fileLimiters;

import database.objects.FileLimiter;

public class IndexFileLimiter extends FileLimiter {
    public IndexFileLimiter(int recordLimit) {
        super(recordLimit);
    }
}
